# This message is used to save the current configuration for an nd_node

# The save_now field is set true to save the current configuration
bool save_now

# The save_rt field provides a convenience mechanism to save each configuration change as it occurs in 
# realtime, ensuring that current configuration is always the saved configuration
bool save_rt