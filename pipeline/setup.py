from setuptools import setup

setup(
    name="pipeline",
    version=0.1,
    description="Numurus SDK Processing Pipeline Library",
    author="Alex McMaster",
    modules=["pipeline"]
    install_requires=["cv"],
)
